package com.Analyser.ENUM;

public enum WebAttackType {

    SQL_INJECTION,
    XSS,
    BRUTE_FORCE,
    DIRECTORY_TRAVERSAL,
    COMMAND_INJECTION,
    WEB_SCANNER,
    FILE_UPLOAD_WEBSHELL,
    LOCAL_FILE_INCLUSION,
    DDOS,
    LFI,
    CSRF,
    NONE   // Default if no attack detected
}

